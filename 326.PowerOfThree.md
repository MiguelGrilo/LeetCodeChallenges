# 326. Power of Three [Challenge](https://leetcode.com/problems/power-of-three/description/)

Given an integer `n`, return `true` *if it is a power of three. Otherwise, return* `false`.

An integer `n` is a power of three, if there exists an integer `x` such that $n == 3^x$.

---

### Example 1:

**Input:** `n = 27`  
**Output:** `true`  
**Explanation:** $27 = 3^3$

### Example 2:

**Input:** `n = 0`  
**Output:** `false`  
**Explanation:** There is no x where $3^x = 0$.

### Example 3:

**Input:** `n = -1`  
**Output:** `false`  
**Explanation:** There is no x where $3^x = (-1)$.

---

### Constraints:

* $-2^{31} \le n \le 2^{31} - 1$

---

<details>
<summary><h2>Solution</h2></summary>

```csharp
public class Solution {
    public bool IsPowerOfThree(int n) {
        // if ( n == 1 ) return true;
        // if ( n <= 0 || n % 3 != 0) return false;

        // return IsPowerOfThree(n/3);

        // Since 3 is a prime number, any power of 3 (like 3^2, 3^5, etc.) 
        // has only 3 as its prime factor.
        // This means the largest possible power of 3 (max3Pow) is perfectly 
        // divisible by any smaller power of 3.
        return n > 0 && 1162261467 % n == 0;
    }
}
```

</details>

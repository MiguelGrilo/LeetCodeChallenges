# 136. Single Number [Challenge](https://leetcode.com/problems/single-number/description/)

Dado um array não vazio de inteiros `nums`, cada elemento aparece duas vezes, exceto um. Encontre esse elemento único.

Tem de implementar uma solução com complexidade de tempo linear e usar apenas espaço extra constante.

---

### Example 1:
**Input:** `nums = [2,2,1]`  
**Output:** `1`  

### Example 2:
**Input:** `nums = [4,1,2,1,2]`  
**Output:** `4`  

### Example 3:
**Input:** `nums = [1]`  
**Output:** `1`  

---

### Constraints:
* `1 <= nums.length <= 3 * 10^4`
* `-3 * 10^4 <= nums[i] <= 3 * 10^4`
* Cada elemento no array aparece duas vezes, exceto um elemento que aparece apenas uma vez.

---

<details>
<summary><h2>Solution</h2></summary>

```csharp
public class Solution {
    public int SingleNumber(int[] nums) {
        int result = 0;
        foreach(int num in nums)
        {
            result ^= num;
        }
        return result;
    }
}
```

#### Other Solution(s):
```csharp
public class Solution {
    public int SingleNumber(int[] nums) {
        List<int> result = new List<int>();
        
        for (int i = 0; i < nums.Length; i++)
        {
            if (result.Contains(nums[i]))
            {
                result.Remove(nums[i]);
            }
            else
            {
                result.Add(nums[i]);
            }
        }
        int[] array = result.ToArray();
        return array[0];
    }
}
```
</details>
